# PostgreSQL Connection Details
PG_HOST=your_postgres_db_host
PG_PORT=5432 # Default is 5432 if not specified
PG_DATABASE=your_postgres_db_name
PG_USER=your_postgres_db_user
PG_PASSWORD=your_postgres_db_password

# Moodle Initial Admin User
MOODLE_USERNAME=admin # Default is admin if not specified
MOODLE_PASSWORD=bitnami # Default is bitnami if not specified
MOODLE_EMAIL=admin@example.com # Default is admin@example.com if not specified

# Moodle Site Configuration
MOODLE_SITE_NAME="My Moodle Site" # Default is "My Moodle Site" if not specified